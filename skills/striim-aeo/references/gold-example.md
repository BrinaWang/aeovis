# Gold Example: An Approved Recommendation

A real recommendation generated by the aeovis platform and **approved by the team**. This is the quality bar. Note what makes it good: every step names exact URLs, exact H2 headings, verbatim panel prompts, named product mechanisms, a "done" definition, effort, and owner. Confidence caveats are stated plainly (the 0.0% baseline rests on ~1 citation in ~11 runs). Nothing in it would survive with the topic swapped out.

## The gap

- Topic: **AI Data Infrastructure** · Type: visibility · Priority 8 · Confidence high
- Striim visibility 0.0% vs. Fivetran 9.1%

## Problem (with diagnosis)

> Striim appears in only 0% of answers for 'AI Data Infrastructure', while Fivetran appears in 9%. Root cause: Striim is absent from 'AI Data Infrastructure' answers not because it loses specific head-to-head questions (0 affected questions were logged), but because it has no page indexed under the AI-infrastructure vocabulary at all — the single cited competitor artifact is a Fivetran category/solution-hub page (the fivetran.com AI/ML + vector/lakehouse destination surface) that defines the category, lists supported AI destinations, and shows the reference architecture in one extractable page, while Striim's corpus is framed entirely as 'CDC / real-time streaming integration.' The 9.1% vs 0.0% split rests on one evidence source and roughly one citation in eleven runs, so the magnitude is low-confidence, but the direction — zero category presence — is a real retrieval-eligibility problem, not a ranking problem.

## Recommended action

> Publish 'AI Data Infrastructure: How Change Data Capture Feeds RAG Pipelines, Vector Databases, and Lakehouse Feature Stores' at striim.com/guides/ai-data-infrastructure as a single-URL category hub to displace Fivetran's AI/ML solution + vector/lakehouse destination page, which is the only competitor artifact cited in this topic.

## Implementation steps (abridged where marked; structure and specificity verbatim)

1. **Create the page** (Low, Content Team) — working title above, at striim.com/guides/ai-data-infrastructure. Notes: what the Fivetran page does that wins today (quotable definitional opening, explicit destination list, reference architecture diagram — a complete self-contained answer on one URL). Constraints: ONE URL, not a nav hub; under /guides/ (evergreen, update-stamped), not /blog/; H1 and first 40 words must contain the literal strings 'AI data infrastructure', 'RAG pipeline', 'vector database', 'lakehouse'. Done = URL reserved, visible "Last updated" stamp in the DOM (not JS-injected), 8 H2s stubbed.
2. **H2: 'What Is AI Data Infrastructure?'** (Medium, Content Team) — 0 evaluated questions were logged for this topic, so register panel prompts verbatim before publish; answer prompts #1–2 ("What is AI data infrastructure?", "What is the data layer for AI and LLM applications?") in the first paragraph: a 45–70 word definition naming the four planes (operational sources, movement/CDC layer, storage plane, serving/context layer), then one sentence placing Striim as the movement layer. Done = the paragraph passes the "quote it with zero surrounding context" test.
3. **H2: 'Where Striim Sits in the AI Data Stack: Reference Architecture'** (Medium, Product Manager) — one diagram: Oracle/SQL Server/PostgreSQL/MySQL/MongoDB → Striim CDC readers → in-flight processing → Pinecone/Milvus/Weaviate/pgvector, Iceberg/Delta, Kafka topics. Diagram needs full text alt AND a numbered text walkthrough (image-only architecture is not extractable). Name CDC mechanisms: Oracle LogMiner/XStream, PostgreSQL logical decoding, MySQL binlog, MongoDB oplog. Done = PM signs off that every arrow maps to a shipping capability.
4. **H2: 'How Do I Get Operational Data Into a Vector Database for RAG?'** (High, Product Manager) — per-target subsections (Pinecone, Milvus, Weaviate, pgvector): change event → upsert, where the embedding call happens, delete propagation, idempotency. Hard rule against fabrication: link live docs for a native writer or state "via Kafka/REST writer + Open Processor" — never imply a nonexistent connector. One copy-pasteable TQL snippet for PostgreSQL→pgvector.
5. **H2: 'How Do I Stream CDC Into an Iceberg Lakehouse for LLM and Agent Workloads?'** (High, Product Manager) — named catalogs (Snowflake Open Catalog/Polaris, Unity Catalog, AWS Glue), merge-on-read vs copy-on-write for CDC, compaction at streaming cadence, schema evolution; name Striim's Snowflake/Databricks/BigQuery/ADLS-S3 writers with docs links.
6. **H2: 'Feature Store and Agent Context Freshness'** (Medium, Content Team) — contrast 5–15 min batch sync vs sub-second CDC with concrete failure modes (fraud score on a 15-minute-old balance; RAG chunk describing an order status that changed 8 minutes ago). Latency table with measured figures; unmeasured numbers labeled as design targets.
7. **H2: 'Supported AI and ML Destinations (Connector-Level Reference)'** (Medium, Product Manager) — the single most displacing section. Real HTML `<table>`: Destination | Category | Delivery mechanism | Docs link | Last verified. Rows ≥: Pinecone, Milvus, Weaviate, pgvector, Iceberg, Delta Lake, Snowflake, BigQuery, Databricks, Kafka. Done = renders with JS disabled, every claim PM-verified.
8. **H2: 'Striim vs. Fivetran for AI Data Infrastructure'** (Medium, Content Team) — cite Fivetran's own published sync tiers and destination list (outbound links to the contrasted source are a corroboration signal). Be explicit where Fivetran is the better fit — one-sided comparisons get discounted. Done = 6–8 dimension table + a two-sentence "choose Striim when / choose Fivetran when" verdict.
9. **H2: FAQ + schema markup** (Medium, Content Team) — 8–10 Q&As using the registered panel prompts verbatim, 40–60 word answers. Article schema (dateModified addresses the freshness mismatch), FAQPage schema on this section only (question text matching visible H3s character-for-character), HowTo on the pgvector snippet. Done = zero errors in Rich Results Test and Schema.org validator.
10. **Internal links** (Low, Content Team) — contextual in-body links from the CDC overview, Snowflake, Databricks/Delta, Kafka integration pages and docs landing, with varied anchors in the target vocabulary; links back from the guide. Done = 5 inbound in-body links, anchors non-duplicated.
11. **Technical review: verify stage-1 crawler accessibility** (Medium, Engineering) — robots.txt allows GPTBot, ClaudeBot, PerplexityBot, OAI-SearchBot, Google-Extended for /guides/ and /docs/; curl with each bot UA and diff server HTML vs rendered DOM (definition, table, FAQ must be server-rendered); no CDN/WAF 403s; sitemap + indexing submission. Done = a log line per bot showing a 200 with full body — the leading-indicator baseline.
12. **Publish, then re-measure with a fixed weekly prompt panel against a site-wide control** (Medium, Content Team) — register the 12 panel prompts, run weekly across GPT/Claude/Perplexity at n≥10, log per-engine query-failure rates (Perplexity silently rejects automated queries — an unreported error rate masquerades as continued 0.0%). Track three stages separately: crawler hits (1–3 weeks), citation share (weeks–months), click-through. Define the target before starting: "named in ≥3 of 12 panel prompts in 2 consecutive weeks," not "3x visibility" from a 0.0% base. First re-measure week 4, decision point week 12.

## Why this was approved (checklist for your own output)

- The diagnosis distinguishes retrieval-eligibility from ranking loss and states its own confidence limits.
- Every step is executable without further research and has a "done" test.
- Product claims have an anti-fabrication rule.
- The comparison is two-sided.
- Measurement follows the panel/control/leading-indicator discipline instead of a single before/after.
